# jackett-sync-arr

## 开始

-   ```shell
      git clone https://github.com/CyberRim/jackett-sync-arr.git
    ```

-   ```shell
      cd jackett-sync-arr
    ```

-   ```shell
      npm install
    ```

-   修改配置文件 config/default.json

-   ```shell
      npm start
    ```
